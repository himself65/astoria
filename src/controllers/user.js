import { handleAPI } from "../utils"

const login = {
  name: 'login',
  methods: {
    post: handleAPI(async () => {
      // todo
    })
  }
}

const logout = {
  name: 'logout',
  methods: {
    post: handleAPI(async () => {
      // todo
    })
  }
}

export {
  login,
  logout
}
